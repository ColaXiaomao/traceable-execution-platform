apiVersion: v1
kind: Secret
metadata:
  name: minio-secret
type: Opaque
stringData:
  MINIO_ROOT_USER: "minioadmin"       # 替换为真实用户名后复制为 secret.yaml
  MINIO_ROOT_PASSWORD: "minioadminpassword"   # 替换为强密码后复制为 secret.yaml
