apiVersion: v1
kind: Service
metadata:
  name: postgres          # 其他 Pod 用这个名字访问：postgres:5432
spec:
  type: ClusterIP         # 仅集群内部可访问（不对外暴露）
  selector:
    app: postgres
  ports:
    - port: 5432
      targetPort: 5432
