apiVersion: v1
kind: Service
metadata:
  name: litellm             # 其他 Pod 用这个名字访问：http://litellm:4000
spec:
  type: ClusterIP
  selector:
    app: litellm
  ports:
    - port: 4000
      targetPort: 4000
